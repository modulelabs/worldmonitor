/**
 * Google Trends global interest index for AI product eras (optional scrub label).
 * Missing catalog → null (missing ≠ invent a score).
 */

export function aiTrendsGlobalAtMs(_ms: number): number | null {
  return null;
}
